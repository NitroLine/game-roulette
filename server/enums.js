const MoveStatus = { OK: "ok", BAD_MOVE: "bad move" };

const GameStatus = {
  WIN: "win", LOSE: "lose", DRAW: "draw", NOTHING: "nothing", TECH_WIN: "technical win"
};

const PlayerSide = { FIRST: "first", SECOND: "second" };

export { MoveStatus, GameStatus, PlayerSide };
